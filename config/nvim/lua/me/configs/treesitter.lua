return {
  ensure_installed = { "lua", "luadoc", "printf", "vim", "vimdoc", "go", "java", "python" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}
